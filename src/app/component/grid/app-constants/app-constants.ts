export class AppConstants {
  static readonly maxZoomIn = 225;
  static readonly maxZoomOut = 16; // The max zoom that the user can zoom out
  static readonly scaleFactor = 1 / 50; // Scales all svg values so that they all are sized correctly at the default zoom.
}
